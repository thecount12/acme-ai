#!/bin/rc
# acme-ai - legacy dispatcher; prefer /acme/bin/aixplain etc directly

echo usage: use /acme/bin/aixplain aianalyze aifix aichat aichat-send >[1=2]
echo same pattern as t+ - select body text, highlight tag command, middle-click
exit usage
