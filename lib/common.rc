#!/bin/rc
# Legacy helper — /acme/bin scripts are flat; use $cputype like aspell/Slide+.

fn findlux {
	luxbin=$home/bin/$cputype/lux
	if(! test -x $luxbin)
		luxbin=$home/bin/lux
	if(! test -x $luxbin)
		luxbin=/bin/lux
	echo $luxbin
}
